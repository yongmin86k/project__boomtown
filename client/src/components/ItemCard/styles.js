const styles = theme => ({
  cardMediaItemsImg: {
    paddingTop: "52%"
  },
  avatar: {
    backgroundColor: theme.palette.primary.main
  },
  cardMediaItemsBtn: {
    padding: theme.spacing(2)
  }
});

export default styles;

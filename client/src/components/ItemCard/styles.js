const styles = theme => ({
  cardGridItem: {
    maxWidth: 500,
    margin: "0 auto"
  },
  cardMediaItemsImg: {
    paddingTop: "52%"
  },
  cardMediaItemsBtn: {
    padding: theme.spacing(2)
  }
});

export default styles;
